server:
  port: 8080
  servlet:
    #Giup dinh vi microservice
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
    password: root
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true